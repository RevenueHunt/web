---
import Base from "../../../layouts/Base.astro";
import { organization, breadcrumb, article, faqPage } from "../../../lib/schema";
import { SITE } from "../../../lib/site";
import { localizedHref } from "../../../lib/i18n";
import {
  REPORT,
  SOURCE,
  HEADLINE,
  GROWTH,
  REVIEWS,
  REVENUE,
  PRICING,
  GEOGRAPHY,
  NICHES,
  QUIZ_LANE,
} from "../../../lib/app-economy";

const pageUrl = new URL(`/de/${REPORT.slug}/`, SITE.site_url).href;
const ogImage = `/og/${REPORT.slug}.png`;

const publishedLabel = new Date(REPORT.datePublished).toLocaleDateString("de-DE", {
  year: "numeric",
  month: "long",
  day: "numeric",
});

const title = "Der Stand der Shopify-App-Ökonomie";
const description =
  "Es gibt 22.546 aktive Apps im Shopify App Store, und fast keine davon kommt irgendwohin. Ein Datenreport über Wachstum, Bewertungen, Umsatzkonzentration, Preise und wer sie baut, auf Basis einer Momentaufnahme jeder aktiven App.";

// Methodik + FAQ, übersetzt. Die einleitende Aussage jeder Methodik-Notiz ist
// inline fett (die englische Quelle hat sie per String-Ersetzung fett gesetzt,
// was übersetzten Text nicht treffen kann). Vor jsonLd definiert, damit
// faqPage(FAQ) sie nutzen kann.
const METHODOLOGY = [
  "<strong>Quelle:</strong> eine Momentaufnahme des öffentlichen Shopify App Store, 22.546 aktive App-Einträge, aufgenommen im Juni 2026. Jedes Feld stammt aus dem öffentlichen Eintrag, nicht aus den internen Daten von Shopify oder dem Konto eines Händlers.",
  "<strong>Der Umsatz ist geschätzt, nicht gemessen.</strong> Er wird aus den veröffentlichten Preisstufen jeder App zusammen mit Bewertungs- und Installationssignalen modelliert. Behandle die Umsatzzahlen als richtungsweisendes Modell dafür, wie konzentriert die Erträge sind, nicht als Abrechnungsdaten. Sie sind überall, wo sie auftauchen, als \"geschätzt\" gekennzeichnet.",
  "<strong>Das Wachstum nutzt das veröffentlichte Launch-Datum jedes Eintrags.</strong> Die Quelle hat rund 2.800 Launch-Daten im Mai 2026 gebündelt, also wird die Kurve als kumulierte aktive Apps pro Jahr gelesen (der Endpunkt ist die tatsächliche aktuelle Gesamtzahl), und die Zahl \"über 6.000 in den letzten 12 Monaten\" bereinigt diesen Einzelmonats-Artefakt.",
  "<strong>Bewertungen sind ein Indikator für Traktion</strong>, kein perfekter: eine großartige App kann neu sein, und Bewertungszahlen hinken der echten Nutzung hinterher. Wir nutzen sie, weil sie das klarste öffentliche, vergleichbare Signal für eine echte Händlerbasis über 22.546 Apps hinweg sind.",
  "Kategorien im App Store sind Funktions-Tags, die eine App sich selbst geben kann, und viele Apps tragen mehrere oder keine. Für die Sektor-Ansicht fassen wir verwandte Tags zu groben Bahnen zusammen und entdoppeln Apps innerhalb jeder; eine App kann zu mehreren gehören, daher überschneiden sich die Sektoren und schließen sich nicht aus, und wir lesen sie als richtungsweisend. Überall sonst stützen wir uns auf die eindeutigen Schnitte (Bewertungen, Preise, Startdatum, Entwicklerland).",
];

const FAQ = [
  {
    question: "Wie viele Apps gibt es im Shopify App Store?",
    answer:
      "22.546 aktive Apps in dieser Momentaufnahme vom Juni 2026. Der Store ist seit 2020 etwa um das Zehnfache gewachsen und hat allein in den letzten 12 Monaten über 6.000 Apps hinzugewonnen, rund 1 von 4 von allem, was heute aktiv ist. KI-gestützte Entwicklung hat aus dem, was früher ein halbjähriges Projekt war, ein Wochenende gemacht.",
  },
  {
    question: "Warum scheitern die meisten Shopify-Apps?",
    answer:
      "Das Bauen ist nicht mehr der Engpass, also wird der Store mit Apps überschwemmt, die nie eine Händlerbasis finden. 51 % haben keine einzige Bewertung und 81 % haben weniger als 10. Das Knappe ist Distribution und Vertrauen: Support beantworten, Bewertungen Händler für Händler verdienen, jahrelang präsent sein. Genau das ist der Teil, den KI dir nicht abnehmen kann.",
  },
  {
    question: "Wie viel verdienen Shopify-Apps?",
    answer:
      "Die Erträge sind extrem konzentriert. Nach unserer Schätzung holt sich das oberste 1 % der Apps (225 davon) rund 72 % des gesamten App-Umsatzes, und allein die ersten 10 holen sich fast 30 %. Nur etwa 36 % der Apps scheinen überhaupt etwas zu verdienen. Der Umsatz ist hier aus öffentlichen Preisen und Nutzungssignalen geschätzt, nicht aus Abrechnungsdaten.",
  },
  {
    question: "Sind die meisten Shopify-Apps kostenlos?",
    answer:
      "Faktisch ja. 46,9 % sind kostenlos und weitere 48,6 % starten mit einer kostenlosen Testphase; nur 4,5 % verlangen Geld im Voraus. Fast 45 % bieten einen dauerhaft kostenlosen Plan. Im Shopify App Store ist ein kostenloser Einstieg keine Wachstumstaktik, er ist die Grunderwartung.",
  },
  {
    question: "Wo kann ich die zugrunde liegenden Daten sehen?",
    answer:
      "Die Zahlen stammen aus einer Momentaufnahme, die wir von jeder aktiven Shopify-App pflegen: Launch-Daten, Bewertungen, Preise, das Built-for-Shopify-Abzeichen und Entwicklerland. Wenn du auf Shopify baust und eine bestimmte Auswertung möchtest, nimm Kontakt auf.",
    answerHtml:
      "Die Zahlen stammen aus einer Momentaufnahme, die wir von jeder aktiven Shopify-App pflegen: Launch-Daten, Bewertungen, Preise, das Built-for-Shopify-Abzeichen und Entwicklerland. Wenn du auf Shopify baust und eine bestimmte Auswertung möchtest, <a href=\"/de/contact/\" class=\"font-medium text-[#16161D] underline decoration-slate-500 underline-offset-2 hover:decoration-[#16161D]\">nimm Kontakt auf</a>.",
  },
];

const jsonLd = [
  organization(),
  breadcrumb([
    { name: "Startseite", url: localizedHref("/", "de") },
    { name: title, url: localizedHref(`/${REPORT.slug}/`, "de") },
  ]),
  article({
    title,
    description,
    url: pageUrl,
    image: ogImage,
    datePublished: REPORT.datePublished,
    dateModified: REPORT.dateModified,
    author: "RevenueHunt",
    tags: [
      "Shopify app store",
      "Shopify-App-Entwickler",
      "App-Store-Statistiken",
      "App-Ökonomie",
    ],
  }),
  {
    "@context": "https://schema.org",
    "@type": "Dataset",
    name: "Der Stand der Shopify-App-Ökonomie 2026",
    description,
    url: pageUrl,
    creator: organization(),
    license: "https://creativecommons.org/licenses/by/4.0/",
    isAccessibleForFree: true,
    datePublished: REPORT.datePublished,
    keywords: [
      "Shopify app store",
      "App-Entwickler",
      "App-Umsatz",
      "App-Bewertungen",
      "ecommerce",
    ],
  },
  faqPage(FAQ),
];

// Section eyebrows for the editorial "field notes" feel.
const E = (n: string, label: string) => `${n} · ${label}`;

// Translated headline cards. Stats stay from the shared data module (HEADLINE[i].stat).
const headlineT = [
  { label: "aktive Apps im Store", note: "Etwa verzehnfacht seit 2020." },
  { label: "haben keine einzige Bewertung", note: "Vier von fünf haben weniger als 10." },
  { label: "des Umsatzes geht an das oberste 1 %", note: "225 Apps. Geschätzt." },
  { label: "verlangen Geld im Voraus", note: "Nur 1 von 20. Fast die Hälfte ist kostenlos." },
];

// Growth caption with the lead phrase bolded.
const growthCaptionHtml =
  "Aktive Apps kumuliert nach Launch-Jahr. <strong>Der Shopify App Store hat sich seit 2020 etwa verzehnfacht</strong> und allein in den letzten 12 Monaten über 6.000 Apps hinzugewonnen, rund 1 von 4 von allem, was heute aktiv ist.";

// Quiz-lane paragraphs (translated), with the live search link and the load-bearing
// lines bolded inline. Caption text mirrors the English source's meaning.
const quizParas = [
  `Unsere eigene Kategorie zeigt dasselbe. <a href="${QUIZ_LANE.searchUrl}" target="_blank" rel="external noopener nofollow" class="font-medium text-[#16161D] underline decoration-slate-500 underline-offset-2 hover:decoration-[#16161D]">die "quiz"-Suche von Shopify</a> liefert 149 Apps, aber hier sammelt sich das Vertrauen an der Spitze genau wie im gesamten Store. Die drei Apps, die zuerst da waren und nie wieder gingen, RevenueHunt, Octane AI und Quiz Kit, vereinen 45 % aller Bewertungen der Kategorie auf sich; <strong>RevenueHunt allein hält rund ein Viertel</strong>, und die untere Hälfte hat keine einzige.`,
  `Wir kamen nicht über den Code an die Spitze. <strong>Bewertungen sind das Symptom der operativen Knochenarbeit</strong> dahinter: sechs Jahre Support beantworten, mit Händlern telefonieren, die nie zahlen würden, und Vertrauen Shop für Shop verdienen. Octane AI und Quiz Kit haben dieselbe harte Arbeit über Jahre geleistet, <strong>und dafür respektieren wir sie</strong>. Ehrenvolle Erwähnung für Lantern und Quizell, beide mit etwas über 120 Bewertungen.`,
];

// Methodology bullets carry their own inline <strong>; render as-is.
const methodologyHtml = METHODOLOGY;
---

<Base
  title={`${title} - Datenreport 2026`}
  description={description}
  ogImage={ogImage}
  jsonLd={jsonLd}
  lang="de"
  translated={true}
>
  <!-- Hero -->
  <section class="bg-gradient-to-b from-slate-50 to-white">
    <div class="mx-auto max-w-5xl px-6 py-16 sm:py-20">
      <nav class="mb-6 text-sm text-slate-500" aria-label="Breadcrumb">
        <a href={localizedHref("/", "de")} class="hover:text-slate-700">Startseite</a>
        <span class="mx-2 text-slate-300">/</span>
        <span class="text-slate-700">Der Stand der Shopify-App-Ökonomie</span>
      </nav>
      <div class="text-center">
        <p class="text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">
          Feldnotizen · {publishedLabel}
        </p>
        <h1 class="mt-3 text-4xl font-bold tracking-tight text-slate-900 sm:text-5xl">
          Der Stand der Shopify-App-Ökonomie
        </h1>
        <p class="mx-auto mt-6 max-w-3xl text-xl text-slate-600">
          <strong>Es gibt 22.546 aktive Apps im Shopify App Store, und fast keine davon kommt irgendwohin.</strong> Wir haben eine Momentaufnahme jeder aktiven App gemacht und dann die Daten zu Wachstum, Bewertungen, Umsatz, Preisen und dazu, wer sie baut, gelesen. Sie erzählen eine einzige Geschichte: Die App zu bauen war nie der schwierige Teil.
        </p>
      </div>
    </div>
  </section>

  <!-- Source band -->
  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6">
      <div class="rounded-2xl bg-[#16161D] px-6 py-10 sm:px-10">
        <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#4dcaff]">
          Die Momentaufnahme
        </p>
        <dl class="mt-6 grid grid-cols-2 gap-8 text-center sm:grid-cols-4">
          <div>
            <dt class="text-4xl font-bold tracking-tight text-white">{SOURCE.apps}</dt>
            <dd class="mt-1 text-sm text-white/70">aktive Apps in der Momentaufnahme</dd>
          </div>
          <div>
            <dt class="text-4xl font-bold tracking-tight text-white">{SOURCE.countries}</dt>
            <dd class="mt-1 text-sm text-white/70">Länder, die sie bauen</dd>
          </div>
          <div>
            <dt class="text-4xl font-bold tracking-tight text-white">{SOURCE.reviewless}</dt>
            <dd class="mt-1 text-sm text-white/70">ohne eine einzige Bewertung</dd>
          </div>
          <div>
            <dt class="text-4xl font-bold tracking-tight text-white">{SOURCE.free}</dt>
            <dd class="mt-1 text-sm text-white/70">komplett kostenlos, ohne bezahlten Plan</dd>
          </div>
        </dl>
        <p class="mx-auto mt-6 max-w-3xl text-center text-sm text-white/60">
          Jede Zahl unten stammt aus dem öffentlichen App-Store-Eintrag aller 22.546 aktiven Apps, Juni 2026. Der Umsatz wird aus veröffentlichten Preisen und Nutzungssignalen geschätzt, nicht aus Abrechnungsdaten, und ist im gesamten Report als solcher gekennzeichnet.
        </p>
      </div>
    </div>
  </section>

  <!-- Headline cards -->
  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <h2 class="text-2xl font-bold tracking-tight text-slate-900 text-center">Die wichtigsten Zahlen</h2>
      <div class="mt-8 grid grid-cols-1 gap-6 sm:grid-cols-2">
        {HEADLINE.map((c, i) => (
          <div class="rounded-xl border border-slate-200 p-7">
            <p class="text-4xl font-bold tracking-tight text-[#16161D]">{c.stat}</p>
            <p class="mt-2 font-medium text-slate-900">{headlineT[i].label}</p>
            <p class="mt-2 text-sm text-slate-600">{headlineT[i].note}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- 01 Growth -->
  <section class="scroll-mt-20 bg-slate-50">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("01", "Wachstum")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">Verzehnfacht seit 2020</h2>
      <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center" set:html={growthCaptionHtml} />
      <div class="mt-8 rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
        <div class="h-72 sm:h-80"><canvas id="c-growth" aria-label="Kumulierte aktive Shopify-Apps nach Jahr" role="img"></canvas></div>
      </div>
    </div>
  </section>

  <!-- 02 Traction -->
  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("02", "Traktion")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">Die meisten Apps bekommen nie eine Bewertung</h2>
      <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center">Die Hälfte des Shopify App Store hat nie eine einzige Bewertung verdient. Vier von fünf Apps haben weniger als 10. Eine Bewertung ist der klarste öffentliche Indikator für einen echten, gehaltenen Händler, und die meisten Apps bekommen nie eine.</p>
      <div class="mt-8 grid items-center gap-8 lg:grid-cols-2">
        <div class="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
          <p class="mb-3 text-center text-sm font-semibold text-slate-900">Apps nach Bewertungsanzahl</p>
          <div class="h-72 sm:h-80"><canvas id="c-reviews" aria-label="Bewertungsverteilung über alle aktiven Apps" role="img"></canvas></div>
        </div>
        <div class="space-y-4">
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="text-3xl font-bold text-[#16161D]">81%</p>
            <p class="mt-1 text-slate-700">haben weniger als 10 Bewertungen</p>
          </div>
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="font-bold text-[#16161D]"><span class="text-3xl">{REVIEWS.medianReviews}</span> <span class="text-sm font-medium text-slate-500">Bewertungen</span></p>
            <p class="mt-1 text-slate-700">im Median, über die Apps, die mindestens eine haben</p>
          </div>
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="font-bold text-[#16161D]"><span class="text-amber-400">★</span> <span class="text-3xl">{REVIEWS.avgRating}</span> <span class="text-sm font-medium text-slate-500">Sterne</span></p>
            <p class="mt-1 text-slate-700">durchschnittliche Bewertung, über die {REVIEWS.ratedCount} Apps, die eine haben</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 03 Revenue -->
  <section class="bg-slate-50">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("03", "Umsatz · geschätzt")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">Das Geld sammelt sich ganz oben</h2>
      <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center">Die geschätzten monatlichen Umsätze sind brutal an der Spitze konzentriert. Das oberste 1 % der Apps (225 davon) holt sich geschätzte 72 %, und allein 10 dieser Apps holen sich fast 30 % des gesamten App-Umsatzes. Nur etwa 36 % der Apps scheinen überhaupt etwas zu verdienen. Und er ballt sich geografisch: <strong>allein die USA und Kanada holen sich geschätzte 57 %</strong> des Umsatzes im Shopify App Store.</p>
      <p class="mx-auto mt-3 max-w-3xl text-sm italic text-slate-500 text-center">Ein Name überragt das gesamte Ökosystem: eine App, die geschätzte 78 % ihres Jahresumsatzes von rund 1 Mrd. US-Dollar von Shopify-Händlern bezieht. Ja, wir meinen dich, <a href='https://www.sec.gov/Archives/edgar/data/1835830/000162828023030618/klaviyoincs-1.htm' target='_blank' rel='external noopener nofollow' class='font-medium text-[#16161D] underline decoration-slate-500 underline-offset-2 hover:decoration-[#16161D]'>Klaviyo</a>.</p>
      <div class="mt-8 grid items-center gap-8 lg:grid-cols-2">
        <div class="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
          <p class="mb-3 text-center text-sm font-semibold text-slate-900">Umsatzverteilung</p>
          <div class="h-64 sm:h-72"><canvas id="c-revenue-donut" aria-label="Aufteilung des geschätzten Umsatzes: die Top 10 Apps, der Rest der Top 1% und die anderen 99%" role="img"></canvas></div>
        </div>
        <div class="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
          <p class="mb-3 text-center text-sm font-semibold text-slate-900">Geschätzter Umsatz nach Land</p>
          <div class="h-64 sm:h-72"><canvas id="c-revenue-countries" aria-label="Geschätzter Umsatz nach Land" role="img"></canvas></div>
        </div>
      </div>
    </div>
  </section>

  <!-- 04 Pricing -->
  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("04", "Preise")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">Fast niemand verlangt Geld im Voraus</h2>
      <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center">Fast die Hälfte des Stores verlangt nichts im Voraus, und weniger als 1 von 20 Apps verlangt Geld vor der ersten Installation. Fast 45 % bieten einen dauerhaft kostenlosen Plan. Freemium ist hier keine Strategie, es ist das Minimum, um überhaupt mitzuspielen.</p>
      <div class="mt-8 grid items-center gap-8 lg:grid-cols-2">
        <div class="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
          <p class="text-center text-sm font-semibold text-slate-900">Einstiegspreise über alle 22.546 aktiven Apps</p>
          <p class="mt-0.5 text-center text-xs text-slate-500">Vier sich gegenseitig ausschließende Stufen, danach, was die App im Voraus verlangt.</p>
          <div class="mt-4 h-72 sm:h-80"><canvas id="c-pricing" aria-label="Verteilung der Preismodelle über alle aktiven Apps, nach Einstiegsstufe" role="img"></canvas></div>
        </div>
        <div class="space-y-4">
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="text-3xl font-bold text-[#16161D]">{PRICING.permanentFree}</p>
            <p class="mt-1 text-slate-700">bieten einen dauerhaft kostenlosen Plan, nicht nur eine Testphase</p>
          </div>
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="text-3xl font-bold text-[#16161D]">{PRICING.completelyFree}</p>
            <p class="mt-1 text-slate-700">sind komplett kostenlos, ohne jeglichen bezahlten Plan</p>
          </div>
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="text-3xl font-bold text-[#16161D]">1 von 20</p>
            <p class="mt-1 text-slate-700">Apps verlangen Geld bei der Installation</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 05 Who builds them -->
  <section class="bg-slate-50">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("05", "Wer sie baut")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">Signal gegen Rauschen</h2>
      <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center">Zwei dominieren: <strong>die USA (24 %) und Indien (15 %) bauen zusammen zwei von fünf</strong> Apps des Stores. Aber <strong>Volumen ist nicht Traktion</strong>. Sortiert man die Länder nur nach den Apps, die 10+ Bewertungen geknackt haben, zeichnet sich die Rangliste neu - <strong>Vietnam</strong>, ein mittelgroßer Entwickler, platziert mehr Apps mit echter Traktion, als seine Größe vermuten lässt, während <strong>Japan und China abrutschen</strong>.</p>
      <div class="mt-8 grid gap-8 lg:grid-cols-2">
        <div class="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
          <p class="text-center text-sm font-semibold text-slate-900">Alle Apps, nach Bewertungsstufe</p>
          <p class="mt-0.5 text-center text-xs text-slate-500">Top 10 Entwickler nach App-Anzahl. Klicke auf eine Stufe, um sie ein- oder auszublenden - blende die Stufen 0 und 1–9 aus und die Rangliste ordnet sich nach Traktion neu: Vietnam, Israel und Singapur steigen auf.</p>
          <div class="mt-4 h-80 sm:h-96"><canvas id="c-geography" aria-label="Wichtigste Entwicklerländer nach Anzahl der Apps, aufgeteilt nach Bewertungsstufe" role="img"></canvas></div>
        </div>
        <div class="grid gap-4 sm:grid-cols-3 lg:grid-cols-1">
          <div class="flex flex-col justify-center rounded-2xl border border-slate-200 bg-white p-6">
            <p class="text-3xl font-bold text-[#16161D]">{GEOGRAPHY.builtForShopify}</p>
            <p class="mt-1 text-sm text-slate-700">tragen das Built-for-Shopify-Abzeichen ({GEOGRAPHY.builtForShopifyCount} Apps), Jahre nachdem Shopify begann, den Standard voranzutreiben</p>
          </div>
          <div class="flex flex-col justify-center rounded-2xl border border-slate-200 bg-white p-6">
            <p class="text-3xl font-bold text-[#16161D]">{GEOGRAPHY.usIndiaShare}</p>
            <p class="mt-1 text-sm text-slate-700">des gesamten Stores werden allein in den USA und Indien gebaut</p>
          </div>
          <div class="flex flex-col justify-center rounded-2xl border border-slate-200 bg-white p-6">
            <p class="text-3xl font-bold text-[#16161D]">{GEOGRAPHY.vietnamSignal}</p>
            <p class="mt-1 text-sm text-slate-700">der Apps aus Vietnam knacken 10+ Bewertungen - die beste Signalrate aller großen Entwickler, etwa doppelt so hoch wie die der USA oder Indiens</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 06 Sectors -->
  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("06", "Sektoren")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">Überfüllt heißt nicht gewinnbar</h2>
      <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center">Die meistbesetzten Bereiche des Stores sind seine schlechtesten Wetten. Entwickler stürzen sich auf Versand und Chat, Allerwelts-Tools, und fast keiner bekommt eine Bewertung: <strong>fast 6 von 10 Drittlogistik-Apps erhalten nie eine einzige</strong>. Die Apps, die durchbrechen, ballen sich dort, wo sich die Arbeit summiert - <strong>Lokalisierung, Preisgestaltung und Anpassung auf Code-Ebene</strong>, die Tools, die unverzichtbar werden, sobald ein Händler sie installiert.</p>
      <div class="mt-8 rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
        <p class="text-center text-sm font-semibold text-slate-900">Alle Sektoren, nach Bewertungsstufe</p>
        <p class="mt-0.5 text-center text-xs text-slate-500">Die Apps jedes Sektors, aufgeteilt nach Bewertungsstufe und vom größten zum kleinsten sortiert.</p>
        <div class="mt-4 h-[26rem] sm:h-[30rem]"><canvas id="c-niches" aria-label="Shopify-App-Sektoren aufgeteilt nach Bewertungsstufe, sortiert nach dem Anteil, der 10 oder mehr Bewertungen erreicht" role="img"></canvas></div>
        <p class="mt-3 text-center text-xs text-slate-400">Sektoren bündeln selbst vergebene Funktions-Tags, pro App entdoppelt. Eine App kann in mehreren liegen, daher überschneiden sich die Sektoren und schließen sich nicht aus - lies sie als grobe Bahnen, nicht als exakte Marktgrößen.</p>
      </div>
      <div class="mt-8 grid gap-4 sm:grid-cols-3">
        <div class="flex flex-col justify-center rounded-2xl border border-slate-200 bg-white p-6">
          <p class="text-3xl font-bold text-[#16161D]">{NICHES.deadNicheZero}</p>
          <p class="mt-1 text-sm text-slate-700">der Drittlogistik-Apps bekommen nie eine einzige Bewertung - die toteste überfüllte Nische im Store</p>
        </div>
        <div class="flex flex-col justify-center rounded-2xl border border-slate-200 bg-white p-6">
          <p class="text-3xl font-bold text-[#16161D]">{NICHES.bestNicheTraction}</p>
          <p class="mt-1 text-sm text-slate-700">der Lokalisierungs-Apps knacken 10+ Bewertungen - die besten Chancen aller großen Sektoren</p>
        </div>
        <div class="flex flex-col justify-center rounded-2xl border border-slate-200 bg-white p-6">
          <p class="text-3xl font-bold text-[#16161D]">{NICHES.topSectorApps}</p>
          <p class="mt-1 text-sm text-slate-700">Apps drängen sich in Versand & Fulfillment, dem größten Sektor - doch nur 28% erreichen Traktion</p>
        </div>
      </div>
    </div>
  </section>

  <!-- 07 The quiz lane -->
  <section class="bg-slate-50">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("07", "Das Quiz-Segment")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">Unsere eigene Kategorie aus der Nähe</h2>
      {quizParas.map((html) => (
        <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center" set:html={html} />
      ))}
      <div class="mx-auto mt-8 grid max-w-3xl grid-cols-3 gap-4 text-center">
        <a
          href="https://apps.shopify.com/product-recommendation-quiz-revenuehunt"
          target="_blank"
          rel="external noopener"
          class="block rounded-xl border border-[#616eff]/60 p-6 shadow-[0_0_28px_-4px_rgba(97,110,255,0.55)] transition hover:border-[#616eff] hover:shadow-[0_0_32px_-2px_rgba(97,110,255,0.7)]"
        >
          <p class="text-sm font-semibold text-slate-900">RevenueHunt</p>
          <p class="mt-1 font-bold text-[#16161D]"><span class="text-3xl">{QUIZ_LANE.revenueHuntReviews}</span> <span class="text-sm font-medium text-slate-500">Bewertungen</span></p>
          <p class="mt-1 text-xs font-semibold text-slate-600"><span class="text-amber-400">★</span> 4,9 Sterne</p>
        </a>
        <div class="rounded-xl border border-slate-200 p-6">
          <p class="text-sm font-medium text-slate-700">Octane AI</p>
          <p class="mt-1 font-bold text-slate-500"><span class="text-3xl">{QUIZ_LANE.octaneReviews}</span> <span class="text-sm font-medium text-slate-500">Bewertungen</span></p>
          <p class="mt-1 text-xs font-semibold text-slate-600"><span class="text-amber-400">★</span> 4,8 Sterne</p>
        </div>
        <div class="rounded-xl border border-slate-200 p-6">
          <p class="text-sm font-medium text-slate-700">Quiz Kit</p>
          <p class="mt-1 font-bold text-slate-500"><span class="text-3xl">{QUIZ_LANE.quizKitReviews}</span> <span class="text-sm font-medium text-slate-500">Bewertungen</span></p>
          <p class="mt-1 text-xs font-semibold text-slate-600"><span class="text-amber-400">★</span> 4,8 Sterne</p>
        </div>
      </div>
      <p class="mx-auto mt-4 max-w-3xl text-center text-sm text-slate-500">Bewertungen im Shopify App Store, Juni 2026. Octane AI und Quiz Kit sind die echten Wettbewerber, die RevenueHunt in dieser Kategorie hat; Respekt an beide.</p>
    </div>
  </section>

  <!-- Takeaway -->
  <section class="bg-[#16161D]">
    <div class="mx-auto max-w-4xl px-6 py-20 text-center">
      <p class="text-xs font-semibold uppercase tracking-[0.2em] text-[#4dcaff]">Das Fazit</p>
      <h2 class="mt-3 text-3xl font-bold tracking-tight text-white sm:text-4xl">
        Die App zu bauen war nie der schwierige Teil.
      </h2>
      <p class="mx-auto mt-5 max-w-2xl text-lg text-white/75">
        KI hat daran nichts geändert. Sie hat es nur unmöglich gemacht, das zu verbergen. Wenn Code zur Massenware wird, ist die Flut neuer Apps nicht die Konkurrenz. Die Konkurrenz ist die Handvoll, die früh auftauchte und nie aufhörte zu ackern: Support beantworten, Bewertungen Händler für Händler verdienen, Vertrauen aufbauen.
      </p>
      <p class="mx-auto mt-5 max-w-2xl text-lg font-semibold text-white">
        Distribution und Vertrauen sind der einzige KI-sichere Zug, der übrig bleibt.
      </p>
      <p class="mx-auto mt-10 max-w-2xl text-lg text-white/85">
        Baust du eine Shopify-App, die wirklich funktioniert? Ich will die Gründer kennenlernen, die sich richtig reinhängen. Lass uns reden.
      </p>
      <div class="mt-6 flex flex-col items-center justify-center gap-3 sm:flex-row">
        <a
          href={localizedHref("/contact/", "de")}
          class="inline-flex items-center justify-center rounded-md bg-white px-6 py-3 text-base font-semibold text-[#16161D] transition hover:bg-slate-100"
        >
          Kontakt aufnehmen
        </a>
        <a
          href="https://www.linkedin.com/in/revenuehunt/"
          target="_blank"
          rel="external noopener"
          class="inline-flex items-center justify-center rounded-md border border-white/30 bg-transparent px-6 py-3 text-base font-semibold text-white transition hover:border-white hover:bg-white/5"
        >
          Auf LinkedIn vernetzen
        </a>
      </div>
      <p class="mx-auto mt-10 max-w-2xl text-white/60">
        Fandest du das interessant? Wir haben dieselbe Analyse in unserer eigenen Kategorie gemacht, im <a href={localizedHref("/state-of-product-recommendation-quizzes/", "de")} class="font-medium text-white underline decoration-white/40 underline-offset-2 hover:decoration-white">Stand der Produktempfehlungs-Quizze</a>.
      </p>
    </div>
  </section>

  <!-- Methodology -->
  <section class="bg-slate-50">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <h2 class="text-2xl font-bold tracking-tight text-slate-900 text-center">Methodik</h2>
      <ul class="mt-8 space-y-5" role="list">
        {methodologyHtml.map((m) => (
          <li class="flex gap-3">
            <span class="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-slate-400" aria-hidden="true"></span>
            <p class="text-slate-700" set:html={m} />
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- FAQ -->
  <section class="bg-white">
    <div class="mx-auto max-w-3xl px-6 py-16">
      <h2 class="text-2xl font-bold tracking-tight text-slate-900 text-center">Häufig gestellte Fragen</h2>
      <div class="mt-10 divide-y divide-slate-200 border-y border-slate-200">
        {FAQ.map((f) => (
          <details class="group">
            <summary class="flex cursor-pointer list-none items-center justify-between gap-4 py-5 text-lg font-semibold text-slate-900 [&::-webkit-details-marker]:hidden">
              <span>{f.question}</span>
              <svg class="h-5 w-5 flex-shrink-0 text-slate-400 transition-transform duration-200 group-open:rotate-180" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 11.17l3.71-3.94a.75.75 0 111.08 1.04l-4.25 4.5a.75.75 0 01-1.08 0l-4.25-4.5a.75.75 0 01.02-1.06z" clip-rule="evenodd" />
              </svg>
            </summary>
            <p class="pb-5 text-slate-700" set:html={f.answerHtml ?? f.answer} />
          </details>
        ))}
      </div>
    </div>
  </section>
</Base>

<script>
  import Chart from "chart.js/auto";
  import { GROWTH, REVIEWS, REVENUE, PRICING, GEOGRAPHY, NICHES, COLORS, CHART_LABELS } from "../../../lib/app-economy";

  Chart.defaults.font.family =
    "Inter, ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif";
  Chart.defaults.color = "#475569";
  Chart.defaults.font.size = 13;

  const el = (id: string) => document.getElementById(id) as HTMLCanvasElement | null;
  const grid = "#eef0f2";
  const kfmt = (v: number) => (v >= 1000 ? v / 1000 + "K" : "" + v);
  const L = CHART_LABELS[document.documentElement.lang] ?? CHART_LABELS.en;

  const growth = el("c-growth");
  if (growth)
    new Chart(growth, {
      type: "line",
      data: {
        labels: GROWTH.labels,
        datasets: [
          {
            data: GROWTH.cumulative,
            borderColor: COLORS.primary,
            backgroundColor: "rgba(97,110,255,0.10)",
            fill: true,
            tension: 0.35,
            borderWidth: 2.5,
            pointRadius: 3,
            pointHoverRadius: 5,
            pointBackgroundColor: COLORS.primary,
          },
        ],
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: { display: false },
          tooltip: { callbacks: { label: (c) => Number(c.parsed.y).toLocaleString() + " apps live" } },
        },
        scales: {
          y: { beginAtZero: true, grid: { color: grid }, ticks: { callback: (v) => kfmt(Number(v)) } },
          x: { grid: { display: false } },
        },
      },
    });

  const donutOpts = (suffix: string) => ({
    responsive: true,
    maintainAspectRatio: false,
    cutout: "62%",
    plugins: {
      legend: { position: "bottom" as const, labels: { boxWidth: 12, boxHeight: 12, padding: 16 } },
      tooltip: { callbacks: { label: (c: any) => `${c.label}: ${c.parsed}${suffix}` } },
    },
  });

  const reviews = el("c-reviews");
  if (reviews)
    new Chart(reviews, {
      type: "doughnut",
      data: { labels: L.reviews, datasets: [{ data: REVIEWS.values, backgroundColor: REVIEWS.colors, borderWidth: 0 }] },
      options: {
        ...donutOpts("%"),
        plugins: {
          legend: { position: "bottom" as const, labels: { boxWidth: 12, boxHeight: 12, padding: 16 } },
          tooltip: {
            callbacks: {
              title: (it: any) => L.reviewsTip.title.replace("{x}", it[0].label),
              label: (c: any) => L.reviewsTip.body.replace("{n}", REVIEWS.counts[c.dataIndex].toLocaleString()).replace("{p}", c.parsed),
            },
          },
        },
      },
    });

  const revDonut = el("c-revenue-donut");
  if (revDonut)
    new Chart(revDonut, {
      type: "doughnut",
      data: { labels: L.revenueDonut, datasets: [{ data: REVENUE.donut.values, backgroundColor: REVENUE.donut.colors, borderWidth: 0 }] },
      options: donutOpts("%"),
    });

  const revCountries = el("c-revenue-countries");
  if (revCountries)
    new Chart(revCountries, {
      type: "doughnut",
      data: { labels: L.revenueCountries, datasets: [{ data: REVENUE.countries.values, backgroundColor: REVENUE.countries.colors, borderWidth: 0 }] },
      options: {
        ...donutOpts("%"),
        plugins: {
          legend: { position: "bottom" as const, labels: { boxWidth: 12, boxHeight: 12, padding: 16 } },
          tooltip: { callbacks: { title: (it: any) => L.revenueCountriesFull[it[0].dataIndex], label: (c: any) => `${L.revenueCountriesFull[c.dataIndex]}: ${c.parsed}%` } },
        },
      },
    });

  const pricing = el("c-pricing");
  if (pricing)
    new Chart(pricing, {
      type: "doughnut",
      data: { labels: L.pricing, datasets: [{ data: PRICING.values, backgroundColor: PRICING.colors, borderWidth: 0 }] },
      options: donutOpts("%"),
    });

  // The "by review tier" chart shows the top 10 builders by whatever tiers are
  // toggled on, drawn from the full GEOGRAPHY.builders pool (16 countries). So
  // toggling tiers can swap countries in and out: showing only "0 reviews"
  // surfaces the biggest noise generators (Pakistan), only "10+" surfaces Israel
  // and Singapore. geoTopTen ranks the pool for a given tier visibility
  // [zero, low, mid, high, vhigh]; applyGeoTop pushes the result into the chart.
  const geoCountry = (en: string) => L.geoCountries?.[en] ?? en;
  const geoPool = GEOGRAPHY.builders.labels.map((label: string, i: number) => ({
    label: geoCountry(label),
    tiers: [GEOGRAPHY.builders.zero[i], GEOGRAPHY.builders.low[i], GEOGRAPHY.builders.mid[i], GEOGRAPHY.builders.high[i], GEOGRAPHY.builders.vhigh[i]],
  }));
  const geoTopTen = (vis: boolean[]) =>
    geoPool
      .map((c: any) => ({
        ...c,
        sum: c.tiers.reduce((s: number, v: number, t: number) => s + (vis[t] ? v : 0), 0),
      }))
      .sort((a: any, b: any) => b.sum - a.sum)
      .slice(0, 10);
  const applyGeoTop = (chart: any) => {
    const top = geoTopTen([0, 1, 2, 3, 4].map((t) => chart.isDatasetVisible(t)));
    chart.data.labels = top.map((c: any) => c.label);
    [0, 1, 2, 3, 4].forEach((t) => {
      chart.data.datasets[t].data = top.map((c: any) => c.tiers[t]);
    });
  };

  const geoTiers = L.geoTiers ?? CHART_LABELS.en.geoTiers!;
  const geoInit = geoTopTen([true, true, true, true, true]);
  const geo = el("c-geography");
  if (geo)
    new Chart(geo, {
      type: "bar",
      data: {
        labels: geoInit.map((c: any) => c.label),
        datasets: [
          { label: geoTiers[0], data: geoInit.map((c: any) => c.tiers[0]), backgroundColor: "#e2e8f0", borderWidth: 0 },
          { label: geoTiers[1], data: geoInit.map((c: any) => c.tiers[1]), backgroundColor: COLORS.gray, borderWidth: 0 },
          { label: geoTiers[2], data: geoInit.map((c: any) => c.tiers[2]), backgroundColor: COLORS.secondary, borderWidth: 0 },
          { label: geoTiers[3], data: geoInit.map((c: any) => c.tiers[3]), backgroundColor: "#579cff", borderWidth: 0 },
          { label: geoTiers[4], data: geoInit.map((c: any) => c.tiers[4]), backgroundColor: COLORS.primary, borderWidth: 0 },
        ],
      },
      options: {
        indexAxis: "y",
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: {
            position: "bottom" as const,
            labels: { boxWidth: 12, boxHeight: 12, padding: 14 },
            onClick(_e, item, legend) {
              const ch = legend.chart;
              const i = item.datasetIndex ?? 0;
              ch.setDatasetVisibility(i, !ch.isDatasetVisible(i));
              applyGeoTop(ch);
              ch.update();
            },
          },
          tooltip: { callbacks: { label: (c) => `${c.dataset.label}: ${Number(c.parsed.x).toLocaleString()}` } },
        },
        scales: {
          x: { stacked: true, beginAtZero: true, grid: { color: grid }, ticks: { callback: (v) => kfmt(Number(v)) } },
          y: { stacked: true, grid: { display: false } },
        },
      },
    });

  // Sectors: every sector's apps stacked by the 5 review tiers, bar length = the
  // sector's total app count. Same absolute view as the geography chart, and like
  // it the bars re-sort (largest first) by the visible total whenever a tier is
  // toggled in the legend. nicheSorted ranks the pool for a tier-visibility mask;
  // applyNicheSort pushes the result (labels + per-tier data) into the chart.
  const nicheLabels = L.niches ?? CHART_LABELS.en.niches!;
  const nichePool = NICHES.tiers.map((tiers: number[], i: number) => ({ label: nicheLabels[i], tiers }));
  const nicheSorted = (vis: boolean[]) =>
    nichePool
      .map((r: any) => ({ ...r, sum: r.tiers.reduce((s: number, v: number, t: number) => s + (vis[t] ? v : 0), 0) }))
      .sort((a: any, b: any) => b.sum - a.sum);
  const nicheRowLabel = (r: any) => r.label;
  const applyNicheSort = (chart: any) => {
    const rows = nicheSorted([0, 1, 2, 3, 4].map((t) => chart.isDatasetVisible(t)));
    chart.data.labels = rows.map(nicheRowLabel);
    [0, 1, 2, 3, 4].forEach((t) => {
      chart.data.datasets[t].data = rows.map((r: any) => r.tiers[t]);
    });
  };
  const niches = el("c-niches");
  if (niches) {
    const nicheInit = nicheSorted([true, true, true, true, true]);
    new Chart(niches, {
      type: "bar",
      data: {
        labels: nicheInit.map(nicheRowLabel),
        datasets: [0, 1, 2, 3, 4].map((t) => ({
          label: geoTiers[t],
          data: nicheInit.map((r: any) => r.tiers[t]),
          backgroundColor: REVIEWS.colors[t],
          borderWidth: 0,
        })),
      },
      options: {
        indexAxis: "y",
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: {
            position: "bottom" as const,
            labels: { boxWidth: 12, boxHeight: 12, padding: 14 },
            onClick(_e, item, legend) {
              const ch = legend.chart;
              const i = item.datasetIndex ?? 0;
              ch.setDatasetVisibility(i, !ch.isDatasetVisible(i));
              applyNicheSort(ch);
              ch.update();
            },
          },
          tooltip: {
            callbacks: {
              label: (c) => {
                const v = Number(c.parsed.x);
                const tot = c.chart.data.datasets.reduce((s: number, d: any, t: number) => s + (c.chart.isDatasetVisible(t) ? Number(d.data[c.dataIndex]) : 0), 0);
                return `${c.dataset.label}: ${v.toLocaleString()} (${tot ? Math.round((100 * v) / tot) : 0}%)`;
              },
            },
          },
        },
        scales: {
          x: { stacked: true, beginAtZero: true, grid: { color: grid }, ticks: { callback: (v) => kfmt(Number(v)) } },
          y: { stacked: true, grid: { display: false } },
        },
      },
    });
  }
</script>
