---
import Base from "../../../layouts/Base.astro";
import Eyebrow from "../../../components/Eyebrow.astro";
import Breadcrumb from "../../../components/Breadcrumb.astro";
import PlanCards from "../../../components/pages/PlanCards.astro";
import Reviews from "../../../components/Reviews.astro";
import { organization, breadcrumb, productWithReviews } from "../../../lib/schema";
import { SITE } from "../../../lib/site";
import { PLANS } from "../../../lib/plans";
import { COMBINED_REVIEWS } from "../../../lib/reviews";
import { localizedHref } from "../../../lib/i18n";

const jsonLd = [
  organization(),
  breadcrumb([
    { name: "דף הבית", url: "/" },
    { name: "תמחור", url: "/pricing/" },
  ]),
  productWithReviews({
    name: "RevenueHunt - חידון המלצת מוצרים",
    description:
      "חידוני המלצת מוצרים לאיקומרס. תוכנית חינמית, תוכניות בתשלום החל מ-$39 לחודש.",
    image: "/og/pricing.png",
    url: "/pricing/",
    ratingValue: COMBINED_REVIEWS.ratingValue,
    reviewCount: COMBINED_REVIEWS.reviewCount,
    reviews: COMBINED_REVIEWS.reviews.map((r) => ({
      author: r.author,
      quote: r.quote,
      rating: r.rating,
      date: r.date,
    })),
    offers: PLANS.map((p) => ({ name: p.name, price: p.price })),
  }),
];
---

<Base
  title="תמחור - RevenueHunt"
  description="תמחור RevenueHunt: תוכנית חינמית זמינה. תוכניות בתשלום החל מ-$39 לחודש. תקופת ניסיון חינם של 15 יום עם תשובות ללא הגבלה. החזר כספי תוך 30 יום."
  jsonLd={jsonLd}
  lang="he"
  translated={true}
>
  <section class="bg-slate-50">
    <div class="mx-auto max-w-7xl px-6 py-16 sm:py-20">
      <Breadcrumb items={[{ label: "דף הבית", href: localizedHref("/", "he") }, { label: "תמחור" }]} />

      <div class="text-center">
        <Eyebrow>תמחור</Eyebrow>
        <h1 class="text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl">
          בחרו את התוכנית הטובה ביותר לאיקומרס שלכם
        </h1>
        <p class="mt-4 text-base text-slate-700 sm:text-lg">
          תוכנית חינמית זמינה. תקופת ניסיון חינם של 15 יום עם תשובות חידון ללא הגבלה וכל הפיצ'רים. החזר כספי תוך 30 יום.
        </p>
      </div>

      <div class="mt-10 flex items-center justify-center gap-3" data-pricing-toggle>
        <span class="text-sm font-semibold text-slate-900" data-billing-label="monthly">חודשי</span>
        <button
          type="button"
          role="switch"
          aria-checked="false"
          aria-label="Toggle yearly billing"
          class="relative inline-flex h-6 w-11 flex-shrink-0 cursor-pointer rounded-full border-2 border-transparent bg-slate-300 transition-colors aria-checked:bg-[#16161D]"
          data-pricing-switch
        >
          <span aria-hidden="true" class="pointer-events-none inline-block h-5 w-5 transform rounded-full bg-white shadow ring-0 transition translate-x-0 [[aria-checked=true]_&]:translate-x-5"></span>
        </button>
        <span class="text-sm text-slate-700">
          <span data-billing-label="yearly" class="font-semibold text-slate-700">שנתי</span>
          <span class="ml-1 inline-flex items-center rounded-full bg-emerald-100 px-2 py-0.5 text-xs font-semibold text-emerald-800">חסכו 20%</span>
        </span>
      </div>

      <div class="mt-8" id="plans">
        <h2 class="sr-only">תוכניות</h2>
        <PlanCards lang="he" />
      </div>
    </div>
  </section>

  <script is:inline>
    (function () {
      const sw = document.querySelector("[data-pricing-switch]");
      if (!sw) return;
      const monthly = document.querySelectorAll('[data-billing="monthly"]');
      const yearly = document.querySelectorAll('[data-billing="yearly"]');
      const monthlyLabel = document.querySelector('[data-billing-label="monthly"]');
      const yearlyLabel = document.querySelector('[data-billing-label="yearly"]');
      sw.addEventListener("click", () => {
        const on = sw.getAttribute("aria-checked") !== "true";
        sw.setAttribute("aria-checked", on ? "true" : "false");
        monthly.forEach((el) => { el.hidden = on; });
        yearly.forEach((el) => { el.hidden = !on; });
        if (monthlyLabel) monthlyLabel.classList.toggle("text-slate-700", on);
        if (monthlyLabel) monthlyLabel.classList.toggle("text-slate-900", !on);
        if (yearlyLabel) yearlyLabel.classList.toggle("text-slate-900", on);
        if (yearlyLabel) yearlyLabel.classList.toggle("text-slate-700", !on);
      });
    })();
  </script>

  <Reviews data={COMBINED_REVIEWS} heading="זוכה לאמון של יותר מ-500 סוחרי איקומרס" bg="bg-white" lang="he" />

  <!-- Enterprise / larger plans - two-column: title+CTA left, 5 checks right -->
  <section id="enterprise" class="mx-auto max-w-7xl px-6 py-12">
    <div class="overflow-hidden rounded-2xl bg-[#16161D] text-white">
      <div class="grid gap-10 p-8 md:grid-cols-2 md:p-12 lg:gap-16">
        <div>
          <p class="text-3xl font-bold tracking-tight sm:text-4xl">צריכים תוכנית גדולה יותר?</p>
          <h2 class="mt-2 text-3xl font-bold tracking-tight sm:text-4xl">שאלו על Enterprise.</h2>
          <a
            href={localizedHref("/demo/", "he")}
            class="mt-10 inline-flex items-center justify-center rounded-md border border-white bg-transparent px-6 py-3 text-base font-semibold text-white transition hover:bg-white/10"
          >
            הזמינו דמו
          </a>
        </div>
        <ul class="space-y-5 md:pt-2">
          <li class="flex gap-3">
            <svg class="mt-1 h-5 w-5 flex-shrink-0 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="5 12 10 17 19 7"/></svg>
            <span class="text-white"><strong class="font-semibold">החל מ-299 USD לחודש.</strong> (תשובות ללא הגבלה לחודש.)</span>
          </li>
          <li class="flex gap-3">
            <svg class="mt-1 h-5 w-5 flex-shrink-0 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="5 12 10 17 19 7"/></svg>
            <span class="text-white">מתוכנן לצמיחה ברמת Shopify Plus. כל הפיצ'רים כלולים.</span>
          </li>
          <li class="flex gap-3">
            <svg class="mt-1 h-5 w-5 flex-shrink-0 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="5 12 10 17 19 7"/></svg>
            <span class="text-white">תמחור מדורג למותגים בנפח גבוה ורב-שווקים</span>
          </li>
          <li class="flex gap-3">
            <svg class="mt-1 h-5 w-5 flex-shrink-0 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="5 12 10 17 19 7"/></svg>
            <span class="text-white">הביאו את החוזים וההסכמים שלכם (SLA, NDA...)</span>
          </li>
          <li class="flex gap-3">
            <svg class="mt-1 h-5 w-5 flex-shrink-0 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="5 12 10 17 19 7"/></svg>
            <span class="text-white">שירותי בניית חידונים שאנחנו עושים עבורכם (תוסף בתשלום)</span>
          </li>
        </ul>
      </div>
    </div>
  </section>

  <!-- Two link rows beneath Enterprise block -->
  <section class="mx-auto max-w-3xl px-6 pb-20 text-center">
    <p class="text-slate-700">
      יש לכם שאלות על התוכניות והתמחור שלנו? - {" "}
      <a
        href="https://docs.revenuehunt.com/customer-success/plans-pricing-faq/"
        target="_blank"
        rel="external noopener"
        class="font-semibold text-slate-900 underline underline-offset-4 hover:text-slate-700"
      >
        עיינו במאמר השאלות הנפוצות הזה
      </a>
    </p>
    <p class="mt-3 text-slate-700">
      צריכים תוכנית עם תשובות חידון ללא הגבלה? - {" "}
      <a href={localizedHref("/demo/", "he")} class="font-semibold text-slate-900 underline underline-offset-4 hover:text-slate-700">
        הזמינו דמו
      </a>
    </p>
  </section>

  <!-- Bottom CTA -->
  <section class="bg-[#16161D]">
    <div class="mx-auto max-w-4xl px-6 py-20 text-center">
      <p class="mb-3 text-sm font-semibold uppercase tracking-wider text-white/70">
        רוב הקונים עוזבים כי הם לא מוצאים את המוצר הנכון
      </p>
      <h2 class="text-3xl font-bold tracking-tight text-white sm:text-4xl">
        הפכו קונים לרוכשים בטוחים עם חידון המלצת מוצרים שמניע מכירות.
      </h2>
      <div class="mt-10 flex flex-col justify-center gap-3 sm:flex-row sm:flex-wrap">
        <a
          href={SITE.signup_url}
          class="inline-flex items-center justify-center rounded-md bg-white px-6 py-3 text-base font-semibold text-[#16161D] transition hover:bg-slate-100"
        >
          הרשמה
        </a>
        <a
          href={localizedHref("/demo/", "he")}
          class="inline-flex items-center justify-center rounded-md border border-white/30 bg-transparent px-6 py-3 text-base font-semibold text-white transition hover:border-white hover:bg-white/5"
        >
          הזמינו דמו
        </a>
      </div>
    </div>
  </section>
</Base>
