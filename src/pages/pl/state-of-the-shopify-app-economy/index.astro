---
import Base from "../../../layouts/Base.astro";
import { organization, breadcrumb, article, faqPage } from "../../../lib/schema";
import { SITE } from "../../../lib/site";
import { localizedHref } from "../../../lib/i18n";
import {
  REPORT,
  SOURCE,
  HEADLINE,
  GROWTH,
  REVIEWS,
  REVENUE,
  PRICING,
  GEOGRAPHY,
  QUIZ_LANE,
} from "../../../lib/app-economy";

const pageUrl = new URL(`/pl/${REPORT.slug}/`, SITE.site_url).href;
const ogImage = `/og/${REPORT.slug}.png`;

const publishedLabel = new Date(REPORT.datePublished).toLocaleDateString("pl-PL", {
  year: "numeric",
  month: "long",
  day: "numeric",
});

const title = "Stan ekonomii aplikacji Shopify";
const description =
  "W Shopify App Store jest 22 546 aktywnych aplikacji i prawie żadna nie osiąga niczego. Raport z danymi o wzroście, recenzjach, koncentracji przychodów, cenach i tym, kto je tworzy, na podstawie migawki każdej aktywnej aplikacji.";

// Methodology + FAQ, translated. The lead clause of each methodology note is
// bolded inline (the English source bolded it via string replace, which cannot
// match translated text). Defined before jsonLd so faqPage(FAQ) can use it.
const METHODOLOGY = [
  "<strong>Źródło:</strong> migawka publicznego Shopify App Store, 22 546 aktywnych ofert aplikacji, wykonana w czerwcu 2026. Każde pole pochodzi z publicznej oferty, nie z wewnętrznych danych Shopify ani z konta jakiegokolwiek sprzedawcy.",
  "<strong>Przychody są szacowane, a nie mierzone.</strong> Są modelowane na podstawie opublikowanych progów cenowych każdej aplikacji w połączeniu z sygnałami recenzji i instalacji. Traktuj liczby przychodów jako kierunkowy model tego, jak skoncentrowane są zarobki, a nie jako dane rozliczeniowe. Są oznaczone jako \"szacunkowe\" wszędzie tam, gdzie się pojawiają.",
  "<strong>Wzrost opiera się na opublikowanej dacie premiery każdej oferty.</strong> Źródło skupiło około 2800 dat premiery w maju 2026, więc krzywą odczytuje się jako skumulowane aktywne aplikacje według roku (punkt końcowy to prawdziwa bieżąca suma), a liczba \"6000+ w ciągu ostatnich 12 miesięcy\" usuwa ten artefakt jednego miesiąca.",
  "<strong>Recenzje są wskaźnikiem trakcji</strong>, niedoskonałym: świetna aplikacja może być nowa, a liczby recenzji są opóźnione względem rzeczywistego użycia. Używamy ich, bo są najwyraźniejszym publicznym, porównywalnym sygnałem prawdziwej bazy sprzedawców na przestrzeni 22 546 aplikacji.",
  "Kategorie w App Store to tagi funkcji, które aplikacja może sama sobie przypisać, a wiele aplikacji nosi ich kilka lub żadnej, więc raportujemy przekroje, które są jednoznaczne (recenzje, ceny, data premiery, kraj dewelopera) i unikamy nadinterpretacji taksonomii tagów.",
];

const FAQ = [
  {
    question: "Ile aplikacji jest w Shopify App Store?",
    answer:
      "22 546 aktywnych aplikacji w tej migawce z czerwca 2026. Sklep wzrósł około 10-krotnie od 2020 roku i dodał ponad 6000 aplikacji tylko w ciągu ostatnich 12 miesięcy, czyli mniej więcej 1 na 4 ze wszystkiego, co jest dziś aktywne. Rozwój wspomagany przez AI zamienił to, co kiedyś było półrocznym projektem, w weekend.",
  },
  {
    question: "Dlaczego większość aplikacji Shopify upada?",
    answer:
      "Tworzenie nie jest już ograniczeniem, więc sklep zalewają aplikacje, które nigdy nie znajdują bazy sprzedawców. 51% nie ma żadnej recenzji, a 81% ma mniej niż 10. Tym, co jest rzadkie, są dystrybucja i zaufanie: odpowiadanie na zgłoszenia, zdobywanie recenzji sprzedawca po sprzedawcy, obecność przez lata. To właśnie tej części AI nie zrobi za Ciebie.",
  },
  {
    question: "Ile zarabiają aplikacje Shopify?",
    answer:
      "Zarobki są skrajnie skoncentrowane. Według naszych szacunków górny 1% aplikacji (225 z nich) zgarnia około 72% wszystkich przychodów aplikacji, a samo pierwsze 10 zgarnia prawie jedną trzecią. Tylko około 36% aplikacji wygląda na takie, które cokolwiek zarabiają. Przychody są tu szacowane na podstawie publicznych cen i sygnałów użycia, a nie danych rozliczeniowych.",
  },
  {
    question: "Czy większość aplikacji Shopify jest darmowa?",
    answer:
      "W praktyce tak. 46,9% jest darmowych, a kolejne 48,6% zaczyna od darmowego okresu próbnego; tylko 4,5% pobiera opłatę z góry. Prawie 45% oferuje stały darmowy plan. W Shopify App Store darmowy punkt wejścia nie jest taktyką wzrostu, jest podstawowym oczekiwaniem.",
  },
  {
    question: "Gdzie mogę zobaczyć dane źródłowe?",
    answer:
      "Ten raport jest pełnodanowym uzupełnieniem naszej notatki z terenu na LinkedIn o ekonomii aplikacji. Liczby pochodzą z migawki, którą utrzymujemy, każdej aktywnej aplikacji Shopify: daty premiery, recenzje, ceny, odznaka Built for Shopify i kraj dewelopera. Jeśli budujesz na Shopify i chcesz konkretny przekrój, skontaktuj się z nami.",
    answerHtml:
      "Ten raport jest pełnodanowym uzupełnieniem naszej notatki z terenu na LinkedIn o ekonomii aplikacji. Liczby pochodzą z migawki, którą utrzymujemy, każdej aktywnej aplikacji Shopify: daty premiery, recenzje, ceny, odznaka Built for Shopify i kraj dewelopera. Jeśli budujesz na Shopify i chcesz konkretny przekrój, <a href=\"/pl/contact/\" class=\"font-medium text-[#16161D] underline decoration-slate-500 underline-offset-2 hover:decoration-[#16161D]\">skontaktuj się z nami</a>.",
  },
];

const jsonLd = [
  organization(),
  breadcrumb([
    { name: "Strona główna", url: localizedHref("/", "pl") },
    { name: title, url: localizedHref(`/${REPORT.slug}/`, "pl") },
  ]),
  article({
    title,
    description,
    url: pageUrl,
    image: ogImage,
    datePublished: REPORT.datePublished,
    dateModified: REPORT.dateModified,
    author: "RevenueHunt",
    tags: [
      "Shopify app store",
      "deweloperzy aplikacji Shopify",
      "statystyki app store",
      "ekonomia aplikacji",
    ],
  }),
  {
    "@context": "https://schema.org",
    "@type": "Dataset",
    name: "Stan ekonomii aplikacji Shopify 2026",
    description,
    url: pageUrl,
    creator: organization(),
    license: "https://creativecommons.org/licenses/by/4.0/",
    isAccessibleForFree: true,
    datePublished: REPORT.datePublished,
    keywords: [
      "Shopify app store",
      "deweloperzy aplikacji",
      "przychody aplikacji",
      "recenzje aplikacji",
      "ecommerce",
    ],
  },
  faqPage(FAQ),
];

// Section eyebrows for the editorial "field notes" feel.
const E = (n: string, label: string) => `${n} · ${label}`;

// Translated headline cards. Stats stay from the shared data module (HEADLINE[i].stat).
const headlineT = [
  { label: "aktywnych aplikacji w sklepie", note: "Około 10-krotny wzrost od 2020 roku." },
  { label: "nie ma ani jednej recenzji", note: "Cztery na pięć mają mniej niż 10." },
  { label: "przychodów zgarnia górny 1%", note: "225 aplikacji. Szacunkowo." },
  { label: "pobiera opłatę z góry", note: "Tylko 1 na 20. Prawie połowa jest darmowa." },
];

// Growth caption with the lead phrase bolded.
const growthCaptionHtml =
  "Aktywne aplikacje skumulowane według roku premiery. <strong>Shopify App Store zanotował około 10-krotny wzrost od 2020 roku</strong> i dodał ponad 6000 aplikacji tylko w ciągu ostatnich 12 miesięcy, czyli mniej więcej 1 na 4 ze wszystkiego, co jest dziś aktywne.";

// Quiz-lane paragraphs (translated), with the live search link and the load-bearing
// lines bolded inline. Caption text mirrors the English source's meaning.
const quizParas = [
  `Nasza własna kategoria pokazuje dokładnie to samo. <a href="${QUIZ_LANE.searchUrl}" target="_blank" rel="external noopener nofollow" class="font-medium text-[#16161D] underline decoration-slate-500 underline-offset-2 hover:decoration-[#16161D]">wyszukiwanie quizów w Shopify</a> zwraca 149 aplikacji, ale zaufanie kumuluje się tu na szczycie dokładnie tak samo jak w całym sklepie. Trzy aplikacje, które przyszły pierwsze i nigdy nie odeszły, RevenueHunt, Octane AI i Quiz Kit, skupiają 45% wszystkich recenzji w kategorii; <strong>sam RevenueHunt ma około jednej czwartej</strong>, a dolna połowa nie ma żadnej.`,
  `Nie dotarliśmy na szczyt dzięki kodowi. <strong>Recenzje to pozostałość po operacyjnej bliznowatej tkance</strong>, która za nimi stoi: sześć lat odpowiadania na zgłoszenia, dzwonienia do sklepów, które nigdy by nie zapłaciły, i zdobywania zaufania sklep po sklepie. Octane AI i Quiz Kit wykonywali tę samą ciężką pracę przez lata, <strong>i szanujemy ich za to</strong>. Wyróżnienie dla Lantern i Quizell, obie z nieco ponad 120 recenzjami.`,
];

// Methodology bullets carry their own inline <strong>; render as-is.
const methodologyHtml = METHODOLOGY;
---

<Base
  title={`${title} - raport z danymi 2026`}
  description={description}
  ogImage={ogImage}
  jsonLd={jsonLd}
  lang="pl"
  translated={true}
>
  <!-- Hero -->
  <section class="bg-gradient-to-b from-slate-50 to-white">
    <div class="mx-auto max-w-5xl px-6 py-16 sm:py-20">
      <nav class="mb-6 text-sm text-slate-500" aria-label="Breadcrumb">
        <a href={localizedHref("/", "pl")} class="hover:text-slate-700">Strona główna</a>
        <span class="mx-2 text-slate-300">/</span>
        <span class="text-slate-700">Stan ekonomii aplikacji Shopify</span>
      </nav>
      <div class="text-center">
        <p class="text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">
          Notatki z terenu · {publishedLabel}
        </p>
        <h1 class="mt-3 text-4xl font-bold tracking-tight text-slate-900 sm:text-5xl">
          Stan ekonomii aplikacji Shopify
        </h1>
        <p class="mx-auto mt-6 max-w-3xl text-xl text-slate-600">
          <strong>W Shopify App Store jest 22 546 aktywnych aplikacji i prawie żadna z nich nie osiąga niczego.</strong> Zrobiliśmy migawkę każdej aktywnej aplikacji, a potem przeanalizowaliśmy dane o wzroście, recenzjach, przychodach, cenach i tym, kto je tworzy. Wszystkie opowiadają jedną historię: stworzenie aplikacji nigdy nie było trudną częścią.
        </p>
      </div>
    </div>
  </section>

  <!-- Source band -->
  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6">
      <div class="rounded-2xl bg-[#16161D] px-6 py-10 sm:px-10">
        <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-white/50">
          Migawka
        </p>
        <dl class="mt-6 grid grid-cols-2 gap-8 text-center sm:grid-cols-4">
          <div>
            <dt class="text-4xl font-bold tracking-tight text-white">{SOURCE.apps}</dt>
            <dd class="mt-1 text-sm text-white/70">aktywnych aplikacji w migawce</dd>
          </div>
          <div>
            <dt class="text-4xl font-bold tracking-tight text-white">{SOURCE.countries}</dt>
            <dd class="mt-1 text-sm text-white/70">krajów, które je tworzą</dd>
          </div>
          <div>
            <dt class="text-4xl font-bold tracking-tight text-white">{SOURCE.reviewless}</dt>
            <dd class="mt-1 text-sm text-white/70">bez żadnej recenzji</dd>
          </div>
          <div>
            <dt class="text-4xl font-bold tracking-tight text-white">{SOURCE.free}</dt>
            <dd class="mt-1 text-sm text-white/70">całkowicie darmowych, bez płatnego planu</dd>
          </div>
        </dl>
        <p class="mx-auto mt-6 max-w-3xl text-center text-sm text-white/60">
          Każda liczba poniżej pochodzi z publicznej oferty w App Store wszystkich 22 546 aktywnych aplikacji, czerwiec 2026. Przychody są szacowane na podstawie opublikowanych cen i sygnałów użycia, a nie danych rozliczeniowych, i są tak oznaczone w całym raporcie.
        </p>
      </div>
    </div>
  </section>

  <!-- Headline cards -->
  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <h2 class="text-2xl font-bold tracking-tight text-slate-900 text-center">Najważniejsze liczby</h2>
      <div class="mt-8 grid grid-cols-1 gap-6 sm:grid-cols-2">
        {HEADLINE.map((c, i) => (
          <div class="rounded-xl border border-slate-200 p-7">
            <p class="text-4xl font-bold tracking-tight text-[#16161D]">{c.stat}</p>
            <p class="mt-2 font-medium text-slate-900">{headlineT[i].label}</p>
            <p class="mt-2 text-sm text-slate-600">{headlineT[i].note}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- 01 Growth -->
  <section class="scroll-mt-20 bg-slate-50">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("01", "Wzrost")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">10-krotny wzrost od 2020 roku</h2>
      <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center" set:html={growthCaptionHtml} />
      <div class="mt-8 rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
        <div class="h-72 sm:h-80"><canvas id="c-growth" aria-label="Skumulowane aktywne aplikacje Shopify według roku" role="img"></canvas></div>
      </div>
    </div>
  </section>

  <!-- 02 Traction -->
  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("02", "Trakcja")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">Większość aplikacji nigdy nie zdobywa recenzji</h2>
      <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center">Połowa Shopify App Store nigdy nie zdobyła ani jednej recenzji. Cztery na pięć aplikacji ma mniej niż 10. Recenzja to najwyraźniejszy publiczny wskaźnik prawdziwego, utrzymanego sklepu, a większość aplikacji nigdy żadnej nie dostaje.</p>
      <div class="mt-8 grid items-center gap-8 lg:grid-cols-2">
        <div class="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
          <div class="h-72 sm:h-80"><canvas id="c-reviews" aria-label="Rozkład recenzji wśród wszystkich aktywnych aplikacji" role="img"></canvas></div>
        </div>
        <div class="space-y-4">
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="text-3xl font-bold text-[#16161D]">81%</p>
            <p class="mt-1 text-slate-700">ma mniej niż 10 recenzji</p>
          </div>
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="font-bold text-[#16161D]"><span class="text-3xl">{REVIEWS.medianReviews}</span> <span class="text-sm font-medium text-slate-500">recenzji</span></p>
            <p class="mt-1 text-slate-700">mediana, wśród aplikacji, które je mają</p>
          </div>
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="font-bold text-[#16161D]"><span class="text-amber-400">★</span> <span class="text-3xl">{REVIEWS.avgRating}</span> <span class="text-sm font-medium text-slate-500">gwiazdki</span></p>
            <p class="mt-1 text-slate-700">średnia ocena, wśród {REVIEWS.ratedCount} aplikacji, które ją mają</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 03 Revenue -->
  <section class="bg-slate-50">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("03", "Przychody · szacunkowo")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">Pieniądze gromadzą się na samym szczycie</h2>
      <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center">Szacowane miesięczne przychody są brutalnie skoncentrowane na górze. Górny 1% aplikacji (225 z nich) zgarnia szacunkowo 72%; same pierwsze 10 zgarnia prawie jedną trzecią. Zaledwie 36% aplikacji wydaje się w ogóle cokolwiek zarabiać.</p>
      <div class="mt-8 grid items-center gap-8 lg:grid-cols-2">
        <div class="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
          <p class="mb-3 text-sm font-medium text-slate-500">Rozkład przychodów</p>
          <div class="h-64 sm:h-72"><canvas id="c-revenue-donut" aria-label="Udział szacowanych przychodów zgarnianych przez górny 1% aplikacji" role="img"></canvas></div>
        </div>
        <div class="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
          <p class="mb-3 text-sm font-medium text-slate-500">Skumulowany udział szacowanych przychodów</p>
          <div class="h-64 sm:h-72"><canvas id="c-revenue-bars" aria-label="Skumulowany udział szacowanych przychodów według wiodących aplikacji" role="img"></canvas></div>
        </div>
      </div>
    </div>
  </section>

  <!-- 04 Pricing -->
  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("04", "Ceny")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">Prawie nikt nie pobiera opłaty z góry</h2>
      <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center">Prawie połowa sklepu nie pobiera niczego z góry, a mniej niż 1 na 20 aplikacji żąda pieniędzy przed pierwszą instalacją. Prawie 45% oferuje stały darmowy plan. Freemium nie jest tu strategią, to absolutne minimum, żeby wejść.</p>
      <div class="mt-8 grid items-center gap-8 lg:grid-cols-2">
        <div class="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
          <div class="h-72 sm:h-80"><canvas id="c-pricing" aria-label="Podział modelu cenowego wśród wszystkich aktywnych aplikacji" role="img"></canvas></div>
        </div>
        <div class="space-y-4">
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="text-3xl font-bold text-[#16161D]">{PRICING.permanentFree}</p>
            <p class="mt-1 text-slate-700">oferują stały darmowy plan, nie tylko okres próbny</p>
          </div>
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="text-3xl font-bold text-[#16161D]">1 na 20</p>
            <p class="mt-1 text-slate-700">aplikacji pobiera pieniądze przed pierwszą instalacją</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 05 Who builds them -->
  <section class="bg-slate-50">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("05", "Kto je tworzy")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">Tworzone w 115 krajach, zdominowane przez dwa</h2>
      <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center">Aplikacje pochodzą ze 115 krajów, ale dwa dominują: USA (16%) i Indie (9%) tworzą wspólnie jedną czwartą sklepu. A po latach, gdy Shopify forsuje standard, tylko 6,7% aktywnych aplikacji (około 1500) nosi odznakę Built for Shopify.</p>
      <div class="mt-8 grid items-center gap-8 lg:grid-cols-2">
        <div class="rounded-2xl border border-slate-200 bg-white p-5 sm:p-7">
          <div class="h-72 sm:h-80"><canvas id="c-geography" aria-label="Główne kraje deweloperów według liczby aplikacji" role="img"></canvas></div>
        </div>
        <div class="space-y-4">
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="text-3xl font-bold text-[#16161D]">{GEOGRAPHY.builtForShopify}</p>
            <p class="mt-1 text-slate-700">nosi odznakę Built for Shopify ({GEOGRAPHY.builtForShopifyCount} aplikacji), lata po tym, jak Shopify zaczął forsować standard</p>
          </div>
          <div class="rounded-xl border border-slate-200 p-6">
            <p class="text-3xl font-bold text-[#16161D]">25%</p>
            <p class="mt-1 text-slate-700">całego sklepu jest tworzone tylko w USA i Indiach</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 06 The quiz lane -->
  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <p class="text-center text-xs font-semibold uppercase tracking-[0.2em] text-[#616eff]">{E("06", "Pas quizów")}</p>
      <h2 class="mt-2 text-2xl font-bold tracking-tight text-slate-900 text-center">Nasza własna kategoria z bliska</h2>
      {quizParas.map((html) => (
        <p class="mx-auto mt-4 max-w-3xl text-slate-700 text-center" set:html={html} />
      ))}
      <div class="mx-auto mt-8 grid max-w-3xl grid-cols-3 gap-4 text-center">
        <a
          href="https://apps.shopify.com/product-recommendation-quiz-revenuehunt"
          target="_blank"
          rel="external noopener"
          class="block rounded-xl border border-[#616eff]/60 p-6 shadow-[0_0_28px_-4px_rgba(97,110,255,0.55)] transition hover:border-[#616eff] hover:shadow-[0_0_32px_-2px_rgba(97,110,255,0.7)]"
        >
          <p class="text-sm font-semibold text-slate-900">RevenueHunt</p>
          <p class="mt-1 font-bold text-[#16161D]"><span class="text-3xl">{QUIZ_LANE.revenueHuntReviews}</span> <span class="text-sm font-medium text-slate-500">recenzji</span></p>
          <p class="mt-1 text-xs font-semibold text-slate-600"><span class="text-amber-400">★</span> 4,9 gwiazdki</p>
        </a>
        <div class="rounded-xl border border-slate-200 p-6">
          <p class="text-sm font-medium text-slate-700">Octane AI</p>
          <p class="mt-1 font-bold text-slate-500"><span class="text-3xl">{QUIZ_LANE.octaneReviews}</span> <span class="text-sm font-medium text-slate-500">recenzji</span></p>
          <p class="mt-1 text-xs font-semibold text-slate-600"><span class="text-amber-400">★</span> 4,8 gwiazdki</p>
        </div>
        <div class="rounded-xl border border-slate-200 p-6">
          <p class="text-sm font-medium text-slate-700">Quiz Kit</p>
          <p class="mt-1 font-bold text-slate-500"><span class="text-3xl">{QUIZ_LANE.quizKitReviews}</span> <span class="text-sm font-medium text-slate-500">recenzji</span></p>
          <p class="mt-1 text-xs font-semibold text-slate-600"><span class="text-amber-400">★</span> 4,8 gwiazdki</p>
        </div>
      </div>
      <p class="mx-auto mt-4 max-w-3xl text-center text-sm text-slate-500">Recenzje w Shopify App Store, czerwiec 2026. Octane AI i Quiz Kit to prawdziwi konkurenci, których RevenueHunt ma w tej kategorii; szacunek dla obu.</p>
    </div>
  </section>

  <!-- Takeaway -->
  <section class="bg-[#16161D]">
    <div class="mx-auto max-w-4xl px-6 py-20 text-center">
      <p class="text-xs font-semibold uppercase tracking-[0.2em] text-[#4dcaff]">Wniosek</p>
      <h2 class="mt-3 text-3xl font-bold tracking-tight text-white sm:text-4xl">
        Stworzenie aplikacji nigdy nie było trudną częścią.
      </h2>
      <p class="mx-auto mt-5 max-w-2xl text-lg text-white/75">
        AI tego nie zmieniło. Sprawiło tylko, że nie da się tego już ukryć. Gdy kod staje się towarem masowym, lawina nowych aplikacji nie jest konkurencją. Konkurencją jest garstka tych, którzy pojawili się wcześnie i nigdy nie przestali harować: odpowiadając na zgłoszenia, zdobywając recenzje sklep po sklepie, budując zaufanie.
      </p>
      <p class="mx-auto mt-5 max-w-2xl text-lg font-semibold text-white">
        Dystrybucja i zaufanie to jedyne zagranie odporne na AI, jakie zostało.
      </p>
      <p class="mx-auto mt-10 max-w-2xl text-lg text-white/85">
        Budujesz aplikację Shopify, która naprawdę działa? Chcę poznać założycieli, którzy robią tę trudną część. Porozmawiajmy.
      </p>
      <div class="mt-6 flex flex-col items-center justify-center gap-3 sm:flex-row">
        <a
          href={localizedHref("/contact/", "pl")}
          class="inline-flex items-center justify-center rounded-md bg-white px-6 py-3 text-base font-semibold text-[#16161D] transition hover:bg-slate-100"
        >
          Skontaktuj się
        </a>
        <a
          href="https://www.linkedin.com/in/revenuehunt/"
          target="_blank"
          rel="external noopener"
          class="inline-flex items-center justify-center rounded-md border border-white/30 bg-transparent px-6 py-3 text-base font-semibold text-white transition hover:border-white hover:bg-white/5"
        >
          Połącz się na LinkedIn
        </a>
      </div>
      <p class="mx-auto mt-10 max-w-2xl text-white/60">
        Ciekawe? Przeprowadziliśmy tę samą analizę w naszej własnej kategorii w <a href={localizedHref("/state-of-product-recommendation-quizzes/", "pl")} class="font-medium text-white underline decoration-white/40 underline-offset-2 hover:decoration-white">stanie quizów rekomendacji produktów</a>.
      </p>
    </div>
  </section>

  <!-- Methodology -->
  <section class="bg-slate-50">
    <div class="mx-auto max-w-5xl px-6 py-16">
      <h2 class="text-2xl font-bold tracking-tight text-slate-900 text-center">Metodologia</h2>
      <ul class="mt-8 space-y-5" role="list">
        {methodologyHtml.map((m) => (
          <li class="flex gap-3">
            <span class="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-slate-400" aria-hidden="true"></span>
            <p class="text-slate-700" set:html={m} />
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- FAQ -->
  <section class="bg-white">
    <div class="mx-auto max-w-3xl px-6 py-16">
      <h2 class="text-2xl font-bold tracking-tight text-slate-900 text-center">Najczęściej zadawane pytania</h2>
      <div class="mt-10 divide-y divide-slate-200 border-y border-slate-200">
        {FAQ.map((f) => (
          <details class="group">
            <summary class="flex cursor-pointer list-none items-center justify-between gap-4 py-5 text-lg font-semibold text-slate-900 [&::-webkit-details-marker]:hidden">
              <span>{f.question}</span>
              <svg class="h-5 w-5 flex-shrink-0 text-slate-400 transition-transform duration-200 group-open:rotate-180" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 11.17l3.71-3.94a.75.75 0 111.08 1.04l-4.25 4.5a.75.75 0 01-1.08 0l-4.25-4.5a.75.75 0 01.02-1.06z" clip-rule="evenodd" />
              </svg>
            </summary>
            <p class="pb-5 text-slate-700" set:html={f.answerHtml ?? f.answer} />
          </details>
        ))}
      </div>
    </div>
  </section>
</Base>

<script>
  import Chart from "chart.js/auto";
  import { GROWTH, REVIEWS, REVENUE, PRICING, GEOGRAPHY, COLORS, CHART_LABELS } from "../../../lib/app-economy";

  Chart.defaults.font.family =
    "Inter, ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif";
  Chart.defaults.color = "#475569";
  Chart.defaults.font.size = 13;

  const el = (id: string) => document.getElementById(id) as HTMLCanvasElement | null;
  const grid = "#eef0f2";
  const kfmt = (v: number) => (v >= 1000 ? v / 1000 + "K" : "" + v);
  const L = CHART_LABELS[document.documentElement.lang] ?? CHART_LABELS.en;

  const growth = el("c-growth");
  if (growth)
    new Chart(growth, {
      type: "line",
      data: {
        labels: GROWTH.labels,
        datasets: [
          {
            data: GROWTH.cumulative,
            borderColor: COLORS.primary,
            backgroundColor: "rgba(97,110,255,0.10)",
            fill: true,
            tension: 0.35,
            borderWidth: 2.5,
            pointRadius: 3,
            pointHoverRadius: 5,
            pointBackgroundColor: COLORS.primary,
          },
        ],
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: { display: false },
          tooltip: { callbacks: { label: (c) => Number(c.parsed.y).toLocaleString() + " apps live" } },
        },
        scales: {
          y: { beginAtZero: true, grid: { color: grid }, ticks: { callback: (v) => kfmt(Number(v)) } },
          x: { grid: { display: false } },
        },
      },
    });

  const donutOpts = (suffix: string) => ({
    responsive: true,
    maintainAspectRatio: false,
    cutout: "62%",
    plugins: {
      legend: { position: "bottom" as const, labels: { boxWidth: 12, boxHeight: 12, padding: 16 } },
      tooltip: { callbacks: { label: (c: any) => `${c.label}: ${c.parsed}${suffix}` } },
    },
  });

  const reviews = el("c-reviews");
  if (reviews)
    new Chart(reviews, {
      type: "doughnut",
      data: { labels: L.reviews, datasets: [{ data: REVIEWS.values, backgroundColor: REVIEWS.colors, borderWidth: 0 }] },
      options: donutOpts("%"),
    });

  const revDonut = el("c-revenue-donut");
  if (revDonut)
    new Chart(revDonut, {
      type: "doughnut",
      data: { labels: L.revenueDonut, datasets: [{ data: REVENUE.donut.values, backgroundColor: REVENUE.donut.colors, borderWidth: 0 }] },
      options: donutOpts("%"),
    });

  const revBars = el("c-revenue-bars");
  if (revBars)
    new Chart(revBars, {
      type: "bar",
      data: { labels: L.revenueBars, datasets: [{ data: REVENUE.bars.values, backgroundColor: COLORS.primary, borderRadius: 4, barThickness: 26 }] },
      options: {
        indexAxis: "y",
        responsive: true,
        maintainAspectRatio: false,
        plugins: { legend: { display: false }, tooltip: { callbacks: { label: (c) => c.parsed.x + "% of estimated revenue" } } },
        scales: { x: { beginAtZero: true, max: 100, grid: { color: grid }, ticks: { callback: (v) => v + "%" } }, y: { grid: { display: false } } },
      },
    });

  const pricing = el("c-pricing");
  if (pricing)
    new Chart(pricing, {
      type: "doughnut",
      data: { labels: L.pricing, datasets: [{ data: PRICING.values, backgroundColor: PRICING.colors, borderWidth: 0 }] },
      options: donutOpts("%"),
    });

  const geo = el("c-geography");
  if (geo)
    new Chart(geo, {
      type: "bar",
      data: { labels: L.geography, datasets: [{ data: GEOGRAPHY.values, backgroundColor: GEOGRAPHY.labels.map((_, i) => (i < 2 ? COLORS.primary : COLORS.secondary)), borderRadius: 4 }] },
      options: {
        indexAxis: "y",
        responsive: true,
        maintainAspectRatio: false,
        plugins: { legend: { display: false }, tooltip: { callbacks: { label: (c) => Number(c.parsed.x).toLocaleString() + " apps" } } },
        scales: { x: { beginAtZero: true, grid: { color: grid }, ticks: { callback: (v) => kfmt(Number(v)) } }, y: { grid: { display: false } } },
      },
    });
</script>
