---
import Base from "../../layouts/Base.astro";
import { organization, breadcrumb, softwareApplication, faqPage } from "../../lib/schema";

const SHOPIFY_INSTALL =
  "https://apps.shopify.com/product-recommendation-quiz-revenuehunt/install?utm_source=revenuehunt.com&utm_medium=referral&utm_campaign=roi_calculator";

// Controls rendered by the calculator. Each gets a paired range + number input,
// synced and recalculated client-side. Defaults are deliberately conservative.
const controls = [
  { id: "visitors", label: "Monthly store visitors", min: 500, max: 200000, step: 500, value: 10000, suffix: "" },
  { id: "aov", label: "Average order value", min: 10, max: 1000, step: 5, value: 60, suffix: "$" },
  { id: "cvr", label: "Current conversion rate", min: 0.1, max: 15, step: 0.1, value: 2, suffix: "%" },
  { id: "engage", label: "Share of visitors who take the quiz", min: 1, max: 100, step: 1, value: 20, suffix: "%" },
  { id: "uplift", label: "Conversion uplift for quiz takers", min: 0, max: 200, step: 5, value: 30, suffix: "%" },
];

const faqs = [
  {
    question: "How is quiz ROI calculated?",
    answer:
      "The calculator compares your baseline monthly revenue (visitors x conversion rate x average order value) against revenue when a share of visitors take a quiz and convert at a higher rate. The difference is the estimated extra revenue. You control every input, including the uplift assumption.",
  },
  {
    question: "What conversion uplift should I expect?",
    answer:
      "It varies by store, traffic quality, and how well the quiz is built. Quiz traffic often converts better than a category page because shoppers get a personalized recommendation, but RevenueHunt does not promise a specific number. Set the uplift field to a figure you can defend, and treat the output as an estimate.",
  },
  {
    question: "Is the calculator free to use?",
    answer:
      "Yes. It is free and ungated, no email required. RevenueHunt itself also has a free plan with 100 quiz responses a month, so you can test the real lift on your own store.",
  },
  {
    question: "How do I actually capture this lift?",
    answer:
      "Build a product recommendation quiz, link each answer to the right products from your catalog, publish it on your store, and capture leads to your email tool. Most merchants launch the same day, and the AI Quiz Copilot can scaffold a full quiz in about 60 seconds.",
  },
];

const jsonLd = [
  organization(),
  softwareApplication(),
  breadcrumb([
    { name: "Home", url: "/" },
    { name: "Quiz ROI calculator", url: "/quiz-roi-calculator/" },
  ]),
  faqPage(faqs),
];
---

<Base
  title="Quiz ROI calculator for ecommerce"
  description="Free, ungated quiz ROI calculator. Estimate the extra monthly and annual revenue a product recommendation quiz could add to your store."
  ogImage="/og/quiz-roi-calculator.png"
  jsonLd={jsonLd}
>
  <!-- Hero -->
  <section class="bg-gradient-to-b from-slate-50 to-white">
    <div class="mx-auto max-w-5xl px-6 py-16 sm:py-20">
      <nav class="mb-6 text-sm text-slate-500" aria-label="Breadcrumb">
        <a href="/" class="hover:text-slate-700">Home</a>
        <span class="mx-2 text-slate-300">/</span>
        <span class="text-slate-700">Quiz ROI calculator</span>
      </nav>
      <div class="text-center">
        <p class="text-sm font-semibold uppercase tracking-wider text-slate-500">Free tool</p>
        <h1 class="mt-3 text-4xl font-bold tracking-tight text-slate-900 sm:text-5xl">Quiz ROI calculator</h1>
        <p class="mx-auto mt-6 max-w-3xl text-xl text-slate-600">
          Estimate the extra revenue a product recommendation quiz could add to your store. Adjust the inputs to match your numbers. No email required.
        </p>
      </div>
    </div>
  </section>

  <!-- Calculator -->
  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6 pb-8">
      <div class="grid gap-8 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm lg:grid-cols-2 lg:p-8">
        <!-- Inputs -->
        <form id="roi-form" class="space-y-6" aria-label="Quiz ROI inputs">
          {controls.map((c) => (
            <div>
              <div class="flex items-center justify-between gap-3">
                <label for={c.id} class="text-sm font-medium text-slate-900">{c.label}</label>
                <div class="flex items-center gap-1 text-sm">
                  {c.suffix === "$" && <span class="text-slate-500">$</span>}
                  <input
                    type="number"
                    id={c.id}
                    min={c.min}
                    max={c.max}
                    step={c.step}
                    value={c.value}
                    class="w-24 rounded-md border border-slate-300 px-2 py-1 text-right font-semibold text-slate-900 focus:border-[#16161D] focus:outline-none focus:ring-1 focus:ring-[#16161D]"
                  />
                  {c.suffix === "%" && <span class="text-slate-500">%</span>}
                </div>
              </div>
              <input
                type="range"
                id={`${c.id}-range`}
                min={c.min}
                max={c.max}
                step={c.step}
                value={c.value}
                class="mt-3 w-full accent-[#16161D]"
                aria-label={`${c.label} slider`}
              />
            </div>
          ))}
        </form>

        <!-- Results -->
        <div class="flex flex-col gap-4 rounded-xl bg-slate-50 p-6">
          <p class="text-sm font-semibold uppercase tracking-wider text-slate-500">Estimated impact</p>
          <div>
            <p class="text-sm text-slate-600">Extra revenue per month</p>
            <p id="out-monthly" class="mt-1 text-4xl font-bold tracking-tight text-[#16161D]">$0</p>
          </div>
          <div class="grid grid-cols-2 gap-4">
            <div class="rounded-lg border border-slate-200 bg-white p-4">
              <p class="text-xs text-slate-600">Extra revenue per year</p>
              <p id="out-annual" class="mt-1 text-xl font-bold text-slate-900">$0</p>
            </div>
            <div class="rounded-lg border border-slate-200 bg-white p-4">
              <p class="text-xs text-slate-600">Extra orders per month</p>
              <p id="out-orders" class="mt-1 text-xl font-bold text-slate-900">0</p>
            </div>
          </div>
          <dl class="mt-2 space-y-1 text-sm text-slate-600">
            <div class="flex justify-between">
              <dt>Baseline monthly revenue</dt>
              <dd id="out-baseline" class="font-medium text-slate-900">$0</dd>
            </div>
            <div class="flex justify-between">
              <dt>With a quiz</dt>
              <dd id="out-withquiz" class="font-medium text-slate-900">$0</dd>
            </div>
          </dl>
          <a
            href={SHOPIFY_INSTALL}
            target="_blank"
            rel="external noopener nofollow"
            onclick="window.gtag&&gtag('event','shopify_app_install',{screen_name:'roi_calculator'})"
            class="mt-2 inline-flex items-center justify-center rounded-md bg-[#16161D] px-6 py-3 text-base font-semibold text-white transition hover:bg-[#26262e]"
          >
            start free on Shopify
          </a>
        </div>
      </div>
      <p class="mt-4 text-xs text-slate-500">
        These are estimates. The conversion uplift is an assumption you set, not a guarantee. RevenueHunt does not promise specific results; actual performance depends on your store, traffic, and quiz.
      </p>
    </div>
  </section>

  <!-- How it works -->
  <section class="bg-slate-50">
    <div class="mx-auto max-w-3xl px-6 py-16">
      <h2 class="text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl">How the calculator works</h2>
      <div class="mt-8 space-y-5 text-lg text-slate-700">
        <p>
          Your baseline monthly revenue is visitors multiplied by conversion rate multiplied by average order value. The calculator then assumes a share of those visitors take a quiz and convert at a higher rate, while the rest convert at your current rate.
        </p>
        <p>
          The gap between the two is the estimated extra revenue. Because a product recommendation quiz sends shoppers to a personalized result instead of a category page, quiz traffic often converts better, which is why the uplift input matters most. Set it to a figure you can defend.
        </p>
        <p>
          A quiz also captures zero-party data and email leads, which compound over time through segmented follow-up. The calculator only models the direct conversion lift, so it tends to understate the full value.
        </p>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="bg-white">
    <div class="mx-auto max-w-3xl px-6 py-16">
      <h2 class="text-3xl font-bold tracking-tight text-slate-900">Frequently asked questions</h2>
      <div class="mt-10 divide-y divide-slate-200 border-y border-slate-200">
        {faqs.map((faq) => (
          <details class="group">
            <summary class="flex cursor-pointer list-none items-center justify-between gap-4 py-5 text-lg font-semibold text-slate-900 [&::-webkit-details-marker]:hidden">
              <span>{faq.question}</span>
              <svg
                class="h-5 w-5 flex-shrink-0 text-slate-400 transition-transform duration-200 group-open:rotate-180"
                viewBox="0 0 20 20"
                fill="currentColor"
                aria-hidden="true"
              >
                <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 11.17l3.71-3.94a.75.75 0 111.08 1.04l-4.25 4.5a.75.75 0 01-1.08 0l-4.25-4.5a.75.75 0 01.02-1.06z" clip-rule="evenodd" />
              </svg>
            </summary>
            <p class="pb-5 text-slate-700">{faq.answer}</p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- Bottom CTA -->
  <section class="bg-[#16161D]">
    <div class="mx-auto max-w-4xl px-6 py-20 text-center">
      <h2 class="text-3xl font-bold tracking-tight text-white sm:text-4xl">
        Put the lift to the test, free.
      </h2>
      <p class="mx-auto mt-4 max-w-2xl text-white/80">
        Build a product recommendation quiz and measure the real impact on your store. Free plan, no credit card.
      </p>
      <div class="mt-10 flex flex-wrap justify-center gap-3">
        <a
          href={SHOPIFY_INSTALL}
          target="_blank"
          rel="external noopener nofollow"
          onclick="window.gtag&&gtag('event','shopify_app_install',{screen_name:'roi_calculator_bottom'})"
          class="inline-flex items-center justify-center rounded-md bg-white px-6 py-3 text-base font-semibold text-[#16161D] transition hover:bg-slate-100"
        >
          start free on Shopify
        </a>
        <a
          href="/pricing/"
          class="inline-flex items-center justify-center rounded-md border border-white/30 bg-transparent px-6 py-3 text-base font-semibold text-white transition hover:border-white hover:bg-white/5"
        >
          see pricing
        </a>
      </div>
    </div>
  </section>

  <script is:inline>
    (function () {
      var KEYS = ["visitors", "aov", "cvr", "engage", "uplift"];
      var usd = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 });
      var num = new Intl.NumberFormat("en-US", { maximumFractionDigits: 0 });
      function clamp(el, v) {
        var min = parseFloat(el.min), max = parseFloat(el.max);
        if (!isNaN(min) && v < min) v = min;
        if (!isNaN(max) && v > max) v = max;
        return v;
      }
      function val(id) {
        var el = document.getElementById(id);
        var v = parseFloat(el.value);
        return isNaN(v) ? 0 : v;
      }
      function set(id, v) {
        var t = document.getElementById(id);
        if (t) t.textContent = v;
      }
      function calc() {
        var V = val("visitors"), A = val("aov"), c = val("cvr") / 100, q = val("engage") / 100, u = val("uplift") / 100;
        var baseline = V * c * A;
        var withQuiz = (V * q * c * (1 + u) + V * (1 - q) * c) * A;
        var extra = Math.max(0, withQuiz - baseline);
        var orders = Math.max(0, V * q * c * u);
        set("out-monthly", usd.format(extra));
        set("out-annual", usd.format(extra * 12));
        set("out-orders", num.format(orders));
        set("out-baseline", usd.format(baseline));
        set("out-withquiz", usd.format(withQuiz));
      }
      KEYS.forEach(function (k) {
        var n = document.getElementById(k);
        var r = document.getElementById(k + "-range");
        if (!n || !r) return;
        n.addEventListener("input", function () { r.value = clamp(n, val(k)); calc(); });
        r.addEventListener("input", function () { n.value = r.value; calc(); });
      });
      calc();
    })();
  </script>
</Base>
